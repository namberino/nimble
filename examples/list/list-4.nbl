fun test(li)
{
    print(li[0]);
    return li[0];
}

var l = test([1, 2, 3]);
print(l);

class Test
{
    init(list)
    {
        this.list = list;
    }

    print_list()
    {
        print(this.list[0]);
    }

    return_list()
    {
        return this.list[0];
    }
}

var t = Test([1, 2, 3]);

t.print_list();

var li = t.return_list();
print(li);

