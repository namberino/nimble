fun loop()
{
    var n = 10;

    while (true)
    {
        if (n <= 2)
        {
            break;
        }

        print(n);
        n = n - 1;
    }

    return n;
}

var n = loop();

print("Output of the function: ");
print(n);
